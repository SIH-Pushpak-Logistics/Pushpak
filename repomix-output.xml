This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
firmware/
  ardupilot_config/
    base_iris.param
    drone_01.param
    drone_02.param
src/
  drone_bringup/
    launch/
      swarm_bringup.launch.py
  navigation_brain/
    navigation_brain/
      __init__.py
      landing_state_node.py
      state_machine_node.py
      vision_nav_node.py
    package.xml
    setup.py
  swarm_utils/
    resource/
      swarm_utils
    swarm_utils/
      __init__.py
      redis_bridge.py
    test/
      test_copyright.py
      test_flake8.py
      test_pep257.py
    package.xml
    setup.cfg
    setup.py
.gitattributes
.gitignore
.repomixignore
docker-compose.yml
Dockerfile
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/swarm_utils/resource/swarm_utils">

</file>

<file path="src/swarm_utils/swarm_utils/__init__.py">

</file>

<file path="src/swarm_utils/swarm_utils/redis_bridge.py">
import redis
import queue
import threading
import time

class RedisTelemetryPublisher:
    def __init__(self, host='localhost', port=6379, stream_name='', logger=None):
        self.stream_name = stream_name
        self.logger = logger
        
        try:
            self.client = redis.Redis(host=host, port=port, decode_responses=True)
            self.client.ping()
            if self.logger:
                self.logger.info(f"Redis Bridge connected. Stream: {self.stream_name}")
        except redis.RedisError as e:
            if self.logger:
                self.logger.error(f"FATAL: Redis connection failed: {e}")
            raise SystemExit

        self._queue = queue.Queue(maxsize=5)
        self._worker = threading.Thread(target=self._publish_loop, daemon=True)
        self._worker.start()

    def _publish_loop(self):
        while True:
            try:
                payload = self._queue.get(block=True, timeout=1.0)
            except queue.Empty:
                continue

            try:
                self.client.xadd(self.stream_name, payload, maxlen=100, approximate=True)
            except redis.RedisError as e:
                if self.logger:
                    self.logger.error(f"Redis publish failed: {e}")
            finally:
                self._queue.task_done()

    def send_velocity_vector(self, drone_id, vx, vy, vz, wz):
        """
        Enforces the data contract. Coerces all floats to strict strings.
        """
        payload = {
            'timestamp': str(time.time()),
            'drone_id': str(drone_id),
            'linear_x': f"{float(vx):.4f}",
            'linear_y': f"{float(vy):.4f}",
            'linear_z': f"{float(vz):.4f}",
            'angular_z': f"{float(wz):.4f}",
        }
        try:
            self._queue.put_nowait(payload)
        except queue.Full:
            pass # Drop frame to maintain real-time edge
</file>

<file path="src/swarm_utils/test/test_copyright.py">
# Copyright 2015 Open Source Robotics Foundation, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

from ament_copyright.main import main
import pytest


# Remove the `skip` decorator once the source file(s) have a copyright header
@pytest.mark.skip(reason='No copyright header has been placed in the generated source file.')
@pytest.mark.copyright
@pytest.mark.linter
def test_copyright():
    rc = main(argv=['.', 'test'])
    assert rc == 0, 'Found errors'
</file>

<file path="src/swarm_utils/test/test_flake8.py">
# Copyright 2017 Open Source Robotics Foundation, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

from ament_flake8.main import main_with_errors
import pytest


@pytest.mark.flake8
@pytest.mark.linter
def test_flake8():
    rc, errors = main_with_errors(argv=[])
    assert rc == 0, \
        'Found %d code style errors / warnings:\n' % len(errors) + \
        '\n'.join(errors)
</file>

<file path="src/swarm_utils/test/test_pep257.py">
# Copyright 2015 Open Source Robotics Foundation, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

from ament_pep257.main import main
import pytest


@pytest.mark.linter
@pytest.mark.pep257
def test_pep257():
    rc = main(argv=['.', 'test'])
    assert rc == 0, 'Found code style errors / warnings'
</file>

<file path="src/swarm_utils/package.xml">
<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>swarm_utils</name>
  <version>0.0.0</version>
  <description>TODO: Package description</description>
  <maintainer email="root@todo.todo">root</maintainer>
  <license>TODO: License declaration</license>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
</file>

<file path="src/swarm_utils/setup.cfg">
[develop]
script_dir=$base/lib/swarm_utils
[install]
install_scripts=$base/lib/swarm_utils
</file>

<file path="src/swarm_utils/setup.py">
from setuptools import find_packages, setup

package_name = 'swarm_utils'

setup(
    name=package_name,
    version='0.0.0',
    packages=find_packages(exclude=['test']),
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='root',
    maintainer_email='root@todo.todo',
    description='TODO: Package description',
    license='TODO: License declaration',
    extras_require={
        'test': [
            'pytest',
        ],
    },
    entry_points={
        'console_scripts': [
        ],
    },
)
</file>

<file path="docker-compose.yml">
services:
  swarm_brain:
    build: .
    container_name: swarm_brain_container
    volumes:
      # This line maps your current Mac folder strictly to /workspace in Linux
      - .:/workspace
    network_mode: host
    tty: true
    stdin_open: true
</file>

<file path="Dockerfile">
# Use the official ROS 2 Humble image (ARM64 compatible)
FROM ros:humble-ros-base

# Install your specific system dependencies
RUN apt-get update && apt-get install -y \
    python3-pip \
    ros-humble-cv-bridge \
    ros-humble-message-filters \
    && rm -rf /var/lib/apt/lists/*

# Install your Python mathematical and network dependencies
RUN pip3 install redis numpy control scipy

# Automatically source ROS 2 when you enter the container
RUN echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc

# Set the working directory
WORKDIR /workspace
</file>

<file path="firmware/ardupilot_config/base_iris.param">
# base_iris.param
# -------------------------------------------------------------
# FRAME CONFIGURATION
# -------------------------------------------------------------
FRAME_CLASS     1      # 1 = Quadcopter
FRAME_TYPE      1      # 1 = X configuration

# -------------------------------------------------------------
# BATTERY SIMULATION (CRITICAL FOR ML TRAINING)
# -------------------------------------------------------------
BATT_MONITOR    4      # 4 = Analog Voltage and Current
BATT_CAPACITY   5000   # Battery capacity in mAh (e.g., 5000mAh)
BATT_VOLT_MAX   16.8   # Max voltage (e.g., 4S LiPo at full charge)
BATT_VOLT_MIN   13.2   # Min safe voltage before critical failsafe
BATT_FS_LOW_ACT 1      # 1 = RTL (Return to Launch) on low battery
BATT_FS_CRT_ACT 2      # 2 = Land on critical battery

# SITL SPECIFIC BATTERY SIMULATION
SIM_BATT_VOLTAGE 16.8  # Starting voltage for the simulator
SIM_BATT_CAP_AH  5.0   # Starting capacity in Amp-Hours
</file>

<file path="firmware/ardupilot_config/drone_01.param">
# drone_01.param
# System ID (CRITICAL: MUST BE UNIQUE)
SYSID_THISMAV   1

# MAVLink configuration for local routing
# Defines where this specific drone outputs its telemetry to MAVROS
SERIAL0_BAUD    115
SERIAL0_PROTOCOL 2
</file>

<file path="firmware/ardupilot_config/drone_02.param">
# drone_02.param
# System ID (CRITICAL: MUST BE UNIQUE)
SYSID_THISMAV   2

# MAVLink configuration for local routing
SERIAL0_BAUD    115
SERIAL0_PROTOCOL 2
</file>

<file path="src/drone_bringup/launch/swarm_bringup.launch.py">
import os
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import DeclareLaunchArgument
from launch.substitutions import LaunchConfiguration

def generate_launch_description():
    # 1. Define the system-wide arguments
    drone_id_arg = DeclareLaunchArgument(
        'drone_id',
        default_value='drone_00',
        description='Unique identifier for the swarm drone'
    )
    
    drone_id = LaunchConfiguration('drone_id')

    # 2. Architect the Node Bringup Sequence
    # This enforces that all nodes share the exact same drone_id namespace
    
    vision_nav_node = Node(
        package='navigation_brain',
        executable='vision_nav_node',
        name='vision_nav',
        parameters=[{'drone_id': drone_id}],
        output='screen'
    )
    
    landing_state_node = Node(
        package='navigation_brain',
        executable='landing_state_node', # Shivam's future executable
        name='landing_state',
        parameters=[{'drone_id': drone_id}],
        output='screen'
    )

    state_machine_node = Node(
        package='navigation_brain',
        executable='state_machine_node',
        name='master_state_machine',
        parameters=[{'drone_id': drone_id}],
        output='screen'
    )

    # 3. Execute the Swarm
    return LaunchDescription([
        drone_id_arg,
        vision_nav_node,
        landing_state_node,
        state_machine_node
    ])
</file>

<file path="src/navigation_brain/navigation_brain/__init__.py">

</file>

<file path="src/navigation_brain/navigation_brain/landing_state_node.py">
#!/usr/bin/env python3
import rclpy
from rclpy.node import Node
from std_msgs.msg import Float32
from swarm_utils.redis_bridge import RedisTelemetryPublisher

class AltitudeNavigationNode(Node):
    def __init__(self):
        super().__init__('altitude_nav_node')
        self.get_logger().info('Initializing Altitude Navigation...')

        # Hardcoded for this node instance, should ideally be a ROS parameter
        self.declare_parameter('drone_id', 'drone_00')
        self.drone_id = self.get_parameter('drone_id').get_parameter_value().string_value
        self.target_altitude = 5.0  # Target Z in meters

        # Input Layer: Strictly Perception/Sensor Data
        self.altitude_subscription = self.create_subscription(
            Float32, 
            '/drone/altitude', 
            self.altitude_callback, 
            10
        )

        # ARCHITECTURE ENFORCEMENT: MAVROS publisher strictly forbidden. 
        # This node calculates intent; it does not authorize motor actuation.

      # --- SHIVAM: YOUR TASK BEGINS HERE ---
        # 1. Instantiate the RedisTelemetryPublisher from swarm_utils.
        #    Target stream: f'telemetry:{self.drone_id}:altitude_cmd'
        pass 

    def altitude_callback(self, msg):
        current_z = msg.data
        
        # --- SHIVAM: YOUR TASK ---
        # 1. Calculate the required $V_z$ (linear_z) to reach self.target_altitude.
        # 2. Use your instantiated RedisTelemetryPublisher to send the vector.
        #    Call .send_velocity_vector(self.drone_id, 0.0, 0.0, calculated_vz, 0.0)
        pass

    def _redis_publisher_worker(self):
        # --- YOUR TASK ---
        #import swarm_utils and use the exact same self.redis_publisher.send_velocity_vector() method,
        #target altitude_cmd stream.
        pass

def main(args=None):
    rclpy.init(args=args)
    node = AltitudeNavigationNode()
    
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        node.get_logger().info("Shutting down Altitude Navigation...")
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
</file>

<file path="src/navigation_brain/package.xml">
<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>navigation_brain</name>
  <version>0.0.1</version>
  <description>Autonomous vision-based navigation node for SIH drone pipeline</description>
  <maintainer email="manager@college.ac.in">Architect</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>cv_bridge</depend>

  <exec_depend>python3-redis</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
</file>

<file path=".gitattributes">
# Enforce standard Unix line endings (LF) for all text files across Mac and Windows
* text=auto eol=lf

# Force specific executable scripts to strictly use LF
*.sh text eol=lf
*.py text eol=lf
*.cpp text eol=lf
*.h text eol=lf
*.hpp text eol=lf

# Denotate binary files that should not be touched by line-ending conversions
*.png binary
*.jpg binary
*.dae binary
*.stl binary
</file>

<file path="README.md">
1. System Objective & Scope
The goal of this system is to achieve decentralized, GPS-independent drone navigation and fleet deconfliction within a Gazebo SITL environment.

Core Constraint: Nodes must be decoupled. Perception and localization do not have direct authority over the motors.

The Bridge: All state intentions are published to a localized Redis Broker.

The Authority: A Master State Machine evaluates network telemetry, applies safety heuristics, and publishes the final kinematic execution to MAVROS.


2. Team Responsibilities & Node Architecture

A. The Perception Layer (Owner: Raunak)
Node: vision_nav_node.py
Role: Calculate X/Y drift velocity using Lucas-Kanade optical flow.
Strict Constraints: * Must use message_filters.ApproximateTimeSynchronizer to time-sync the camera frames (/camera/image_raw) with the altimeter (/drone/altitude) to accurately perform the pinhole camera conversion ($V_x = \frac{u \cdot Z}{f_x}$).
Must utilize the queue.Queue(maxsize=5) background thread for Redis I/O.
Forbidden: Subscribing to or publishing to /mavros/setpoint_velocity/cmd_vel.

B. The Altitude & Landing Layer (Owner: Shivam)
Node: landing_state_node.py
Role: Calculate the Z-axis velocity required to hold altitude or execute a precision landing.
Strict Constraints:
Must read from the altimeter and output a proportional $V_z$ command.
Must clamp maximum ascent/descent speeds to prevent simulation physics instability.
Forbidden: Publishing directly to MAVROS. Output must go strictly to the designated Redis stream.

C. The Machine Learning Data Pipeline (Owners: Bhavya & Priya)

Script: telemetry_logger.py
Role: Ingest simulation state data to build the dataset for the non-linear battery degradation routing model.
Strict Constraints:
Do not interface with ROS 2 DDS.
Subscribe directly to the Redis telemetry:*:velocity streams and log outputs to normalized CSVs at 10Hz.

D. The Simulation Environment (Owner: Aditya)

Platform: Blender to Gazebo Classic.
Strict Constraints:
Scale: 1 Blender Unit = 1 Real-World Meter.
Performance: Low-poly meshes only. All transforms must be applied (Origin at 0,0,0 geometric base).
Format: Export strictly as .dae (Collada) with high-contrast ground textures for optical flow validation.

E. The Master State Machine & Overwatch (Owner: Lead Architect)

Node: state_machine_node.py
Role: The absolute brain of the drone. Ingests all Redis streams, calculates staleness, handles decentralized swarm collision overrides, and executes motor commands.

3. The Data Contract (Stream & Topic Registry)
This is the immutable data structure of the system. Any deviation in naming conventions or JSON payloads will result in immediate node failure.

ROS 2 Subscriptions (Input Layer)
| Topic Name | Type | Consumer | Purpose |
| :--- | :--- | :--- | :--- |
| `/camera/image_raw` | `sensor_msgs/Image` | Perception | Raw visual feed. |
| `/drone/altitude` | `std_msgs/Float32` | Perception, Landing | Metric altitude (Z). |



Redis Broker Streams (The Decoupled Network)
1. X/Y Vision Telemetry
Stream Key: telemetry:<drone_id>:velocity        
JSON Schema:{
  "timestamp": "float (UNIX epoch)",
  "drone_id": "string",
  "linear_x": "float",
  "linear_y": "float",
  "linear_z": "0.0",
  "angular_z": "0.0"
}

2. Z-Axis Landing Telemetry

Stream Key: telemetry:<drone_id>:altitude_cmd
JSON Schema: Identical to above, utilized exclusively for linear_z instructions.

3. Global Swarm Override
Stream Key: emergency_override:<drone_id>
JSON Schema: Identical to above. Dictated by the off-board distributed engine.
ROS 2 Publishers (Execution Layer)

| Topic Name | Type | Producer | Purpose |
| :--- | :--- | :--- | :--- |
| `/mavros/setpoint_velocity/cmd_vel` | `geometry_msgs/TwistStamped` | State Machine | Final unarguable flight controller input. |

4. Failsafes & Execution Priorities
The state_machine_node.py operates at 20Hz and adheres to the following strict priority hierarchy.

Priority 1: Swarm Override. If a valid, non-stale vector exists in emergency_override:<drone_id>, execute it immediately and ignore all local perception.

Priority 2: Local Perception Integration. Pull the latest X/Y vector from telemetry:<drone_id>:velocity and the Z vector from telemetry:<drone_id>:altitude_cmd. Merge them into a single TwistStamped message.

Priority 3: The Staleness Failsafe. The state machine compares the payload timestamp against the system clock. If the delta exceeds 0.5 seconds, the data is stale. The drone drops the command and defaults to a zero-velocity hover to prevent uncontrolled drift.
</file>

<file path="src/navigation_brain/navigation_brain/state_machine_node.py">
#!/usr/bin/env python3
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import TwistStamped
import redis
import time

class StateMachineNode(Node):
    def __init__(self):
        super().__init__('state_machine_node')
        
        self.declare_parameter('drone_id', 'drone_00')
        self.drone_id = self.get_parameter('drone_id').get_parameter_value().string_value

        try:
            self.redis_client = redis.Redis(host='localhost', port=6379, decode_responses=True)
            self.redis_client.ping()
            self.telemetry_stream = f'telemetry:{self.drone_id}:velocity'
            self.altitude_stream = f'telemetry:{self.drone_id}:altitude_cmd'
            self.override_stream = f'emergency_override:{self.drone_id}'
        except redis.ConnectionError as e:
            self.get_logger().error(f'FATAL: Redis connection failed: {e}')
            raise SystemExit
        
        self.get_logger().info(f'Initializing Master State Machine for {self.drone_id}...')

        # The ONLY node allowed to talk to MAVROS
        self.velocity_publisher = self.create_publisher(
            TwistStamped,
            '/mavros/setpoint_velocity/cmd_vel',
            10
        )



        self.control_timer = self.create_timer(0.05, self.control_loop) # 20Hz
        self.max_data_age = 0.5 

    def control_loop(self):
        cmd_msg = TwistStamped()
        cmd_msg.header.stamp = self.get_clock().now().to_msg()
        cmd_msg.header.frame_id = f'{self.drone_id}_base_link'
        
        target_vx, target_vy, target_vz, target_wz = 0.0, 0.0, 0.0, 0.0
        
        # 1. Pull the absolute newest data from all three streams
        override_data = self.redis_client.xrevrange(self.override_stream, max='+', min='-', count=1)
        vision_data = self.redis_client.xrevrange(self.telemetry_stream, max='+', min='-', count=1)
        altitude_data = self.redis_client.xrevrange(self.altitude_stream, max='+', min='-', count=1)

        # 2. Priority 1: Global Override dictates everything
        if override_data and not self.is_stale(override_data[0][1].get('timestamp', 0)):
            target_vx, target_vy, target_vz, target_wz = self.extract_vectors(override_data[0][1])
                
        # 3. Priority 2: Merge Local Vision (X/Y) and Local Altitude (Z)
        else:
            # Extract X/Y from Raunak's vision node
            if vision_data and not self.is_stale(vision_data[0][1].get('timestamp', 0)):
                payload = vision_data[0][1]
                target_vx = float(payload.get('linear_x', 0.0))
                target_vy = float(payload.get('linear_y', 0.0))
                target_wz = float(payload.get('angular_z', 0.0))
            
            # Extract Z from Shivam's altitude node
            if altitude_data and not self.is_stale(altitude_data[0][1].get('timestamp', 0)):
                payload = altitude_data[0][1]
                target_vz = float(payload.get('linear_z', 0.0))

        # 4. Execute
        cmd_msg.twist.linear.x = target_vx
        cmd_msg.twist.linear.y = target_vy
        cmd_msg.twist.linear.z = target_vz
        cmd_msg.twist.angular.z = target_wz

        self.velocity_publisher.publish(cmd_msg)

    def extract_vectors(self, payload):
        return (
            float(payload.get('linear_x', 0.0)),
            float(payload.get('linear_y', 0.0)),
            float(payload.get('linear_z', 0.0)),
            float(payload.get('angular_z', 0.0))
        )

    def is_stale(self, payload_timestamp_str):
        try:
            if (time.time() - float(payload_timestamp_str)) > self.max_data_age:
                self.get_logger().warn('Stale data. Defaulting to hover.')
                return True
            return False
        except ValueError:
            return True

def main(args=None):
    rclpy.init(args=args)
    node = StateMachineNode()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
</file>

<file path="src/navigation_brain/navigation_brain/vision_nav_node.py">
#!/usr/bin/env python3
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import Image
from std_msgs.msg import Float32
from geometry_msgs.msg import TwistStamped
from cv_bridge import CvBridge, CvBridgeError
import cv2
import message_filters
from swarm_utils.redis_bridge import RedisTelemetryPublisher



class VisionNavigationNode(Node):
    def __init__(self):
        super().__init__('vision_nav_node')
        
        # --- Swarm Identity ---
        self.declare_parameter('drone_id', 'drone_00')
        self.drone_id = self.get_parameter('drone_id').get_parameter_value().string_value
        self.get_logger().info(f'Initializing Vision Navigation Node for {self.drone_id}...')

        self.bridge = CvBridge()

        # --- Redis Setup ---
        self.redis_publisher = RedisTelemetryPublisher(
            stream_name=f'telemetry:{self.drone_id}:velocity', 
            logger=self.get_logger()
        )

        # --- Synchronized Inputs ---
        self.camera_sub = message_filters.Subscriber(self, Image, '/camera/image_raw')
        self.altitude_sub = message_filters.Subscriber(self, Float32, '/drone/altitude')
        
        # Exact time synchronization (0.05s slop)
        self.ts = message_filters.ApproximateTimeSynchronizer(
            [self.camera_sub, self.altitude_sub], 
            queue_size=10, 
            slop=0.05
        )
        self.ts.registerCallback(self.synchronized_callback)

    def synchronized_callback(self, image_msg, alt_msg):
        """
        Callback only fires when a camera frame and altitude reading match in time.
        """
        try:
            cv_image = self.bridge.imgmsg_to_cv2(image_msg, desired_encoding='bgr8')
        except CvBridgeError as e:
            self.get_logger().error(f'CvBridge Failure: {str(e)}')
            return

        current_altitude = alt_msg.data

        # Run vision pipeline
        velocity_vector = self.process_vision_pipeline(cv_image, current_altitude)

        # Send telemetry payload via the delegated publisher class
        self.redis_publisher.send_velocity_vector(
            self.drone_id,
            velocity_vector.twist.linear.x,
            velocity_vector.twist.linear.y,
            velocity_vector.twist.linear.z,
            velocity_vector.twist.angular.z
        )

    def process_vision_pipeline(self, cv_frame, current_altitude):
        """
        RAUNAK: Your OpenCV math goes here. 
        You now have current_altitude (Z) perfectly synced with the frame to do your pinhole math.
        """
        cmd_msg = TwistStamped()
        cmd_msg.header.stamp = self.get_clock().now().to_msg()
        cmd_msg.header.frame_id = f'{self.drone_id}_base_link'

        cmd_msg.twist.linear.x = 0.0
        cmd_msg.twist.linear.y = 0.0
        cmd_msg.twist.linear.z = 0.0
        cmd_msg.twist.angular.z = 0.0

        # TODO: OpenCV Lucas-Kanade and Pinhole Conversion
        
        return cmd_msg

def main(args=None):
    rclpy.init(args=args)
    node = VisionNavigationNode()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        node.get_logger().info('Shutting down.')
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
</file>

<file path="src/navigation_brain/setup.py">
from setuptools import find_packages, setup

package_name = 'navigation_brain'

setup(
    name=package_name,
    version='0.0.1',
    packages=find_packages(exclude=['test']),
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='Architect',
    maintainer_email='manager@college.ac.in',
    description='Autonomous vision-based navigation node for SIH drone pipeline',
    license='Apache-2.0',
    tests_require=['pytest'],
    entry_points={
        'console_scripts': [
            'vision_nav_node = navigation_brain.vision_nav_node:main',
            'state_machine_node = navigation_brain.state_machine_node:main',
            'landing_state_node = navigation_brain.landing_state_node:main'

        ],
    },
)
</file>

<file path=".gitignore">
# =========================
# ROS 2 Workspace Artifacts
# =========================
build/
install/
log/
core.*

# =========================
# Python
# =========================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.env
.venv/
pip-log.txt
pip-delete-this-directory.txt

# =========================
# OS Generated Files
# =========================
# Your Mac
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Their Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# =========================
# IDEs and Editors
# =========================
.vscode/
.idea/
*.swp
*.swo
*~
</file>

<file path=".repomixignore">
# Ignore ROS 2 build artifacts and logs
build/
install/
log/

# Ignore heavy 3D mesh files and textures (AI cannot read these)
*.world
*.dae
*.stl
*.png
*.jpg
*.jpeg
*.mtl
*.obj

# Ignore standard Python cache
__pycache__/
*.pyc
</file>

</files>
